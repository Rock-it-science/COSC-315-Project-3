int main(){
    int n;  /* the n lowest significant bits that represent the offset */
    int m;  /* the next m bits that represent the page number; assume that n+m is always 16 */
    int v1; /* first virtual address that needs to be mapped to a page number and offset */
    int v2; /* second virtual address */
    int addr[128]; /* a sequence of virtual addresses, one per line until the end of the file */

    
}